# fuin-fx-jacoco

Aggregates the JaCoCo coverage of all other modules into a single report that SonarCloud reads. Internal to
the build - it is not deployed and contains no code beyond the one class Maven needs to accept the module.
